# ===================================================================
# BuhBot Frontend Environment Configuration
# ===================================================================
# Copy this file to .env.local and fill in your values
# ===================================================================

# Supabase Configuration (optional in DEV MODE)
# Get from: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# API Configuration
NEXT_PUBLIC_API_URL=

# Backend URL for Next.js rewrites (server-side only)
# Used by next.config.ts to proxy /api/* requests to backend
# Default: http://localhost:3001
BACKEND_URL=http://localhost:3001

# ===================================================================
# DEV MODE (Local Development without Supabase)
# ===================================================================
# If NEXT_PUBLIC_SUPABASE_ANON_KEY is not set and NODE_ENV=development,
# the app runs in DEV MODE:
# - Authentication is bypassed
# - A mock admin user session is used
# - All API calls include X-Dev-Mode header
#
# To use DEV MODE:
# 1. Leave NEXT_PUBLIC_SUPABASE_ANON_KEY empty
# 2. Set BACKEND_URL to your local backend (default: http://localhost:3001)
# 3. Run: npm run dev
# ===================================================================
