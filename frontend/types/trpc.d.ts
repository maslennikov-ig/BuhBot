/**
 * tRPC Type Definitions
 *
 * Import AppRouter type from backend for end-to-end type safety.
 * This file is a placeholder until backend router is properly exported.
 *
 * @module types/trpc
 */

// TODO: Import actual AppRouter from backend when monorepo is configured
// import type { AppRouter } from '@buhbot/backend/src/api/trpc/router';

// Placeholder type - will be replaced with actual backend types
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export type AppRouter = {};

// Re-export for convenience
export type { AppRouter as Router };
